#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - print all possible combinations
 * of two digits
 *
 * Return: 0 if Success
 */
int main(void)
{
	int i;
	int j;

	for (i = 0; i < 10; i++)
	{
		for (j = i + 1; j <= 9; j++)
		{
			putchar((ij);
		}
		if (i != 89)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}

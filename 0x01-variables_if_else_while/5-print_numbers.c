#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - prints all single digit of base 10
 *
 * Return: 0 if Success
 */
int main(void)
{
	for (int i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf('\n');
	return (0);
}

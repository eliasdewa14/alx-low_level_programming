#include "main.h"

/**
 * _isdigit - print if the character is 0 through 9
 * @c: input parameter for testing
 *
 * Return: 1 if it is digit, otherwise 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
